/*2. Crie uma arrow function que receba um número e retorne seu fatorial. */

(numero) => {
    var soma = 1
    while (numero>0){
        soma = soma * numero
        numero--;
  }
  return soma

} 
 console.log(numero = 9)